\chapter{Initial Prototypes}
\label{chap:initial-prototypes}

\textbf{maybe this chapter makes more sense after implementation? it requires a lot of design philosophy fundamentals that i'll talk about in design/implementation}

As part of this research, a significant portion of time was dedicated towards a particular approach in solving this disseration's research question.
This chapter will describe the motivations behind the chosen approach, the implementation details, and then evaluate 
the results and describe why the experiment yielded unsatisfactory results. 
It is worth dedicating time to this, as the approach involves the use of popular datasets 
that others may use in solving similar problems to this research.

\section{Design Motivation}

\textbf{TODO - this may need to get re-written depending on my final RQ, ngl it really depends}
\textbf{also it's pretty poorly-worded so could do with some re-writing}

Before describing the motivation behind this design, it is worth revisiting our Research Question: 
\emph{``Can a Recommender System assist an Irish student in exploring college courses they would consider studying?''}.
In other words, we want to design an RS that recommends college courses that the student would realistically consider studying.
The RS can be designed to recommend ``safe" college courses that the RS is sure the user will like - after all,
\textbf{relevance} is one of the qualities of an effective RS, as described in subsection \ref{sec:rs-fundamentals}, 
so an RS with this design philosophy will satisfy one of these qualities.
However, another quality of an effective RS is \textbf{serendipity}; the gold standard for RS's.
Among the three types of RS's described in subsection \ref{sec:rs-fundamentals},
\emph{Collaborative Filtering} is more likely to provide \textbf{serendipitous}\footnote{According to \url{https://developers.google.com/machine-learning/recommendation/collaborative/summary}}
recommendations, because by leveraging the data of what other similar users also liked, 
rather than solely relying on what the user likes,
the RS may make recommendations that are relevant but less expected.

\section{Datasets}

\textbf{todo}

\subsection{Datasets and initial challenges}

\textbf{TODO???} In section \ref{sec:research-gap} and chapter \ref{chap:design}, we justified a research 
gap in leveraging a user's RIASEC model and Academic Interests to provide personalised college course recommendations.
In the previous section, we established that Collaborative Filtering (CF) may be utilised in providing serendipitous recommendations to the user.
However, in order to design a CF RS in this context, we need to find datasets with \textbf{other users' college choice,} and either their Academic Interests 
or RIASEC model.
To the best of our knowledge, this kind of data is not available in the Irish context.
As a result, some time was spent searching for datasets outside of the Irish context.\\

During our research, the most promising dataset was published by OpenPsychometrics\footnote{\url{https://openpsychometrics.org/_rawdata/}} with data on over 140,000 individuals.
In this dataset, which will be referred to as the `RIASEC dataset` henceforth, are the results of a 48-question quiz which determined the user's RIASEC model, by asking the user how much they enjoyed certain activities on a 5-point Likert scale.
An example of a question is given in figure \ref{fig:op-quiz}.
Most importantly, as part of the quiz the user was asked to provide their college major.
After some data cleaning, we were left with the results of over 70,000 user's RIASEC question results and college majors.
This kind of data would be essential in designing a CF RS.

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.5]{images/op-question.png}
    \caption{An example of a question given on the OpenPsychometrics RIASEC quiz.}
    \label{fig:op-quiz}
\end{figure}

However, as described in subsection \ref{sec:research-gap}, this disseration seeks to design an RS that recommends specific Irish college courses 
as opposed to general areas of study.
The RIASEC dataset has been answered by users from all over the world, particularly from the USA.
As a result, a challenge is posed in mapping the college majors that users provided in the RIASEC dataset, 
to specific Irish college courses.
Moreover, new Irish college courses are being created each year\footnote{See footnote \ref{label:new-courses}},
compounding the difficulty of the problem.\\

\subsection{Solution to introduced problems}

As a solution to this problem of specificity, we can create more general categories of related college courses - for example, we can 
group Medicine, Nursing and Occupational Therapy all under a ``healthcare" category.
The college majors in the RIASEC dataset can be placed into these categories,
and the college courses in the Irish college course dataset can also be more easily placed into these categories.
Admittedly, a disadvantage of this solution is a loss of nuance and context in the mapping.
In other words, specific traits of particular college majors 
will get lost as they are combined into a more general category with other college majors.\\

A popular dataset on college majors in the USA\footnote{\url{https://github.com/fivethirtyeight/data/tree/master/college-majors}} 
was most appropriately applied to this problem. This dataset, which will be referred to as the `college major dataset' henceforth, initially featured 174 college majors and categorised each college major 
under 15 categories.
However, in it's initial form, this college major dataset failed to capture many of the college majors provided in the RIASEC dataset.
In addition to this, many of the college major categories were too broad, and instead were split up into different college major categories.
As a result, this college major dataset underwent significant manual revisions, and the final form of this dataset is provided in \textbf{TODO - appendix!}.
After the manual revisions, the college major dataset featured over 200 college majors with 16 categories.\\

We now have a college major dataset with 200 college majors and placing them into 16 college major categories, 
and a RIASEC dataset with over 70,000 user's college majors and the results of their RIASEC quiz.
Using a pre-processing script as given in \textbf{TODO - appendix!},
we can categorise each of the $\sim$70,000 users in the RIASEC dataset into one of 16 college major categories as provided in the college major dataset.
This augmented dataset will be instrumental in training a Machine Learning model to predict a user's college major from the results
of this RIASEC quiz.

\subsection{Machine Learning Model}

From the work in the last subsection, we now have a dataset with over 70,000 user's RIASEC quiz results, and their college major category.
This can be reconceptualised in the context of a Machine Learning (ML) problem: 
we have 48 input features, and 1 output variable.
In this case, each input feature is a rating from 1--5, indicating the user's enjoyment towards a particular task,
and the output is one of 16 college major categories.\\

Many different ML models were trained on this data, and they were tested on unseen data to gather evaluation metrics,
in particular, top-1 and top-3 accuracy.
The ML model with the best\footnote{The evaluation metrics of other models are not reported as they are not deemed relevant to the content of this chapter.} 
top-1 and top-3 accuracy was the Histogram-based Gradient Boosting Classifier (HGBC), with it's implementation details given in \textbf{todo - appendix!}.
This implementation of a HGBC achieved a top-1 accuracy of \textbf{0.319}, and a top-3 accuracy of \textbf{0.606.}
In other words for the unseen test set, this HGBC implementation predicted the user's college major correctly in 31.9\% of cases (top-1 accuracy), 
and in 60.6\% of cases, the model predicted the user's college major correctly in it's first 3 predictions (top-3 accuracy).

\subsection{Model Evaluation}

The top-1 and top-3 accuracy scores for the unseen test set were reported in the previous subsection.
However, in this section we will take a closer look in evaluating the chosen model's performance.\\

In figure \ref{fig:confusion-matrix} below, we can observe a Confusion Matrix of the chosen model 

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.5]{images/recall-scores.png}
    \caption{An example of a question given on the OpenPsychometrics RIASEC quiz.}
    \label{fig:confusion-matrix}
\end{figure}

\subsection{Limitations of the approach}

% show accuracy scores of different models being trained on the data 
    % top-3 and top-1 accuracy
    % pick the best model - hgbm
% show the confusion matrix
    % highlight its difficulty with certain sections, e.g. law, humanities, languages, etc.
% this is because the questions of the dataset are not exhaustive!
    % no questions that were really targeting these categories, making it very difficult for the model to predict


% attempted to "fill in this gap" by asking the user for their LC subjects
    % each LC subject contributed points towards an academic interests and RIASEC quality
    % this was inspired by the conversation with GC's (ref next section)


